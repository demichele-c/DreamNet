<h1>{{name}}</h1>
<p>{{description}}</p>

{{#if image_url}}
  <h2>Your Dream Image</h2>
  <a href="{{image_url}}" target="_blank">
    <img src="{{image_url}}" alt="Generated Dream Image" style="max-width: 100%;">
  </a>
{{else}}
  <p>No image available for this dream.</p>
{{/if}}

<a href="/dreams">Back to Dreams</a>
